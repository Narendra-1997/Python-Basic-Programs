"""define a function which can print a dictionary where
the keys are numbers between 1and 20 (both included and the values are square of keys
hint___
use dict[keys=value pattern to put entry into dictionary """
#use ** operator to get power of a number.
# use range ()for loop

def narendra():
    d=dict()
    for name in range(1,21):
        d[name]=name**2
    print(d)
    return d 
mani=narendra()

    
        